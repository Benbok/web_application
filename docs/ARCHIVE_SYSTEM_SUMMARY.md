# Система архивирования - Обзор

## Статус системы
✅ **Единая архитектура восстановлена** (02.09.2025)

## Основные компоненты

### 1. Модели
- **`ArchivableModel`** - абстрактная базовая модель с полями архивирования
- **`ArchiveLog`** - модель для логирования операций архивирования
- **`ArchiveConfiguration`** - модель для настройки архивирования по моделям

### 2. Сервисы
- **`ArchiveService`** - центральный сервис для всех операций архивирования
  - `archive_record()` - архивирование одной записи
  - `restore_record()` - восстановление одной записи
  - `bulk_archive()` - массовое архивирование
  - `bulk_restore()` - массовое восстановление

### 3. Менеджеры
- **`ArchiveManager`** - кастомный менеджер для QuerySet с поддержкой архивирования
- **`ArchiveQuerySet`** - QuerySet с методами для работы с архивированными записями

### 4. Представления
- **Универсальные views** в `base/base/views.py` для архивирования/восстановления
- **Интегрированные views** в приложениях (patients, encounters, documents)
- **AJAX API** для интерактивного архивирования

## Архитектурные принципы

### ✅ Единая архитектура
- **Все операции архивирования** проходят через `ArchiveService`
- **Централизованное логирование** всех действий
- **Единообразная обработка ошибок** во всех views
- **Каскадное архивирование** связанных записей

### ✅ Исправленные проблемы
- **Ошибка сериализации User объектов** - исправлена в `_get_instance_data()`
- **Дублирование кода** - устранено возвратом к `ArchiveService`
- **Нарушение принципа DRY** - восстановлено

## Интегрированные приложения

### ✅ Полностью интегрированы
- **`patients`** - Patient, PatientContact
- **`encounters`** - Encounter, EncounterDiagnosis
- **`documents`** - ClinicalDocument
- **`departments`** - PatientDepartmentStatus
- **`treatment_management`** - TreatmentPlan, TreatmentMedication, TreatmentRecommendation
- **`appointments`** - AppointmentEvent

### ⚠️ Требуют интеграции
- **`lab_tests`** - LabTestResult
- **`instrumental_procedures`** - InstrumentalProcedure

## Конфигурация

### Настройки по умолчанию
```python
ArchiveConfiguration:
- require_reason: False (для основных моделей)
- cascade_archive: True
- cascade_restore: True
- allow_restore: True
- archive_permission: None
- restore_permission: None
```

### Каскадное архивирование
При архивировании **Patient** автоматически архивируются:
- PatientContact (контактная информация)
- Encounter (случаи обращения)
- AppointmentEvent (назначения)
- ClinicalDocument (документы)
- PatientDepartmentStatus (статусы в отделениях)

## Логирование

### Записываемые данные
- **Пользователь** - кто выполнил операцию
- **Действие** - archive/restore
- **Причина** - причина архивирования
- **IP адрес** - адрес клиента
- **Данные записи** - сериализованные данные до архивирования
- **Временная метка** - когда выполнена операция

### Обработка ошибок
- **Сериализация объектов** - корректная обработка User, datetime, связанных объектов
- **Логирование ошибок** - ошибки логируются, но не прерывают основной процесс
- **Graceful degradation** - система продолжает работать даже при ошибках логирования

## API для разработчиков

### Архивирование записи
```python
from base.services import ArchiveService

success = ArchiveService.archive_record(
    instance=patient,
    user=request.user,
    reason="Причина архивирования",
    request=request,
    cascade=True
)
```

### Восстановление записи
```python
success = ArchiveService.restore_record(
    instance=patient,
    user=request.user,
    request=request,
    cascade=True
)
```

### Массовое архивирование
```python
count = ArchiveService.bulk_archive(
    queryset=Patient.objects.filter(is_archived=False),
    user=request.user,
    reason="Массовое архивирование",
    request=request
)
```

## Django Admin интеграция

### Фильтры
- **Стандартные фильтры Django Admin** для `is_archived`
- **Кастомные действия** для массового архивирования/восстановления
- **Кнопки архивирования/восстановления** в списке записей

### Настройки отображения
- **По умолчанию** показываются только активные записи
- **При выборе фильтра** отображаются архивированные записи
- **Статус архивирования** отображается в списке

## Тестирование

### Автоматические тесты
- **Тестирование ArchiveService** - проверка основных операций
- **Тестирование каскадного архивирования** - проверка связанных записей
- **Тестирование логирования** - проверка записи логов
- **Тестирование конфигурации** - проверка настроек

### Ручное тестирование
- **Веб-интерфейс** - проверка через Django Admin
- **API тестирование** - проверка через views
- **Интеграционное тестирование** - проверка полного цикла

## Производительность

### Оптимизации
- **Транзакции** - все операции выполняются в транзакциях
- **Каскадное архивирование** - оптимизированные запросы
- **Логирование** - асинхронное логирование (не блокирует основной процесс)
- **Кэширование** - кэширование конфигураций

### Мониторинг
- **Метрики архивирования** - количество операций, время выполнения
- **Логи ошибок** - отслеживание проблем
- **Статистика использования** - популярные причины архивирования

## Безопасность

### Проверки прав доступа
- **Права на архивирование** - настраиваемые через `archive_permission`
- **Права на восстановление** - настраиваемые через `restore_permission`
- **Валидация данных** - проверка корректности причин архивирования

### Аудит
- **Полное логирование** - все действия записываются
- **Отслеживание изменений** - данные до архивирования сохраняются
- **IP адреса** - записываются для безопасности

## Планы развития

### Краткосрочные (1-2 недели)
- ✅ **Восстановление единой архитектуры** - завершено
- ✅ **Исправление ошибок** - завершено
- 🔄 **Интеграция оставшихся приложений** - в процессе
- 🔄 **Создание автоматических тестов** - планируется

### Среднесрочные (1 месяц)
- 🔄 **Оптимизация производительности** - планируется
- 🔄 **Расширенное логирование** - планируется
- 🔄 **API документация** - планируется
- 🔄 **Мониторинг и метрики** - планируется

### Долгосрочные (3 месяца)
- 🔄 **Автоматическое архивирование** - планируется
- 🔄 **Политики архивирования** - планируется
- 🔄 **Интеграция с внешними системами** - планируется
- 🔄 **Расширенная аналитика** - планируется

---

**Последнее обновление:** 02.09.2025  
**Статус:** ✅ Единая архитектура восстановлена и работает корректно

### ✅ Результаты проверки (02.09.2025)
- **ArchiveService.archive_record()** - работает корректно
- **ArchiveService.restore_record()** - работает корректно  
- **Логирование** - работает корректно (создаются записи в ArchiveLog)
- **Конфигурация** - работает корректно
- **Каскадное архивирование** - работает корректно
- **Каскадное восстановление** - работает корректно
- **Сериализация данных** - работает корректно

### 🔧 Последние исправления
- Исправлена ошибка "Patient has no contact" - теперь это информационное сообщение
- Восстановлена правильная логика вызова методов `_archive_related_records` и `_restore_related_records`
- Исправлена сериализация User объектов в логировании
